/**
 * commonjs
 * exports = module.exports
 * require
 * 
 * 
 * es6
 * import
 * export
 * export default
 */

export function square(x) {
 return Math.pow(x, 2);
}

export function cube(x) {
 return Math.pow(x, 3);
}
